import 'package:flutter/material.dart';

class BitcoinTrackerScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Bitcoin Tracker")),
      body: Center(child: Text("Hello World")),
    );
  }
}