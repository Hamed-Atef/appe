import 'package:flutter/material.dart';


class FlashChatScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Flash Chat")),
      body: Center(child: Text("Hello World")),
    );
  }
}