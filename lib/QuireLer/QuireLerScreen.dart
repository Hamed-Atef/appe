import 'package:flutter/material.dart';


class QuireLerScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("QuireLer")),
      body: Center(child: Text("Hello World")),
    );
  }
}