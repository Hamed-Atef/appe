import 'package:flutter/material.dart';

class ClimeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Clime")),
      body: Center(child: Text("Hello World")),
    );
  }
}