import 'package:flutter/material.dart';


class DestiniScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Destini")),
      body: Center(child: Text("Hello World")),
    );
  }
}